import { observer } from "mobx-react-lite";
import company from "../../utils/stores/company";
import "./TopicListPage.scss";

const TopicListPage: React.FC = observer(() => {
  return <div>TopicListPage</div>;
});

export default TopicListPage;
